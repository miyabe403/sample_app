<div class="list-wrap">
<% @lists.each do |list| %>
  <div class="list-box">
    <% if list.image.attached? %>
      <%= image_tag list.image, size: "200x200" %>
    <% else %>
      <%= image_tag 'no_image', size: "200x200"%>
    <% end%>
    <div class="list-comment">
      <span class=list-body""><%= list.body %></span>  <!-- %= list.body % はListモデルのbodyカラムに入っているデータを呼び出している記述です。 -->
      <span class="list-title"><%=link_to list.title, list_path(list.id) %></span>
    </div>
  </div>
<% end %>  
</div>

<!-- レイアウトを整えるため以下を削除 -->
<!--<h1>投稿一覧</h1>-->
<!-- 各レコードの内容は、listという変数に格納されます。 -->
<!--% @lists.each do |list| %>-->
<!-- @lists.eachと記述することで、Listレコードの数だけ繰り返し処理を行います。 -->
  <!--<p>タイトル</p>-->
  <!--<span>-->  <!-- list.[カラム名]ここではlist.titleと記述することで、レコードのカラムの内容を表示できます。 -->
    <!--%= link_to list.title, list_path(list.id) %>-->  <!--「:id」がどんな値なのかを指定しないといけないため、listsには引数を指定します。指定する値はレコードのidカラムです。 -->
  <!--</span>-->

  <!--<p>画像</p>--> <!-- imgタグを生成するimage_tagヘルパーを使い、画像の表示を行います。 -->
  <!-- バリデーションを設定後 以下を削除 -->
  <!--% if list.image.attached? %> <!-- これは、list.imageに対して.attached?というimageが存在するかを確認するメソッドを呼び出しています。-->
    <!--%= image_tag list.image, size: "200x200" %> <!-- listに紐ついているimageをimage_tagに渡すことで画像を表示する。画像サイズを200x200に設定 -->
  <!--% else %> <!-- imageが存在していなければ用意したno_imageを表示する -->
    <!--%= image_tag 'no_image', size: "200x200" %>
  <!--% end %>

  <!-- バリデーションを設定後以下を追加 -->
  <!--%= image_tag list.image, size: "200x200" %>-->

<!--% end %>-->